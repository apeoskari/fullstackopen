{
  "name": "puhelinluettelo",
  "version": "0.0.1",
  "description": "",
  "license": "MIT",
  "author": "Aaro Kuusinen",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "test": "echo \\\"Error: no test specified\\\" && exit 1",
    "lint": "eslint .",
    "build:ui": "rimraf dist && cd frontend && npm run build && cpy dist --recursive",
    "deploy:full": "npm run build:ui && git add . && git commit -m uibuild && git push"
  },
  "dependencies": {
    "dotenv": "^17.2.1",
    "express": "^5.1.0",
    "mongoose": "^8.17.0",
    "morgan": "^1.10.1"
  },
  "devDependencies": {
    "@eslint/js": "^9.33.0",
    "@stylistic/eslint-plugin-js": "^4.4.1",
    "cpy-cli": "^6.0.0",
    "eslint": "^9.33.0",
    "globals": "^16.3.0",
    "rimraf": "^6.0.1"
  }
}
